import { TradesPage } from './TradesPage';

export function OpenPositionsPage() {
  return <TradesPage fixedStatus="open" title="Open Positions" subtitle="Currently open trades across all users" />;
}
